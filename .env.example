# Backend API base URL (used at runtime by the browser).
# No trailing slash. Example for Vercel: https://medcare-backend.vercel.app
VITE_API_BASE=
# Optional: only used by the Vite dev server proxy (npm run dev) to forward
# /api and /actuator requests. Ignored in production builds.
VITE_API_ORIGIN=